# URL do endpoint WebSocket do backend Java.
# Em ambiente Docker, substitua pelo nome do serviço definido no docker-compose
# (ex: http://backend:8080/ws-time-trial).
# Em desenvolvimento local, o valor padrão http://localhost:8080/ws-time-trial é usado.
VITE_WS_URL=http://localhost:8080/ws-time-trial
